//
//  SettingsView.swift
//  FFill
//
//  Placeholder — full implementation in Phase 6 (extension instructions + import/export).
//

import SwiftUI

struct SettingsView: View {
    var body: some View {
        ContentUnavailableView(
            "Settings",
            systemImage: "gear"
        )
        .navigationTitle("Settings")
    }
}
